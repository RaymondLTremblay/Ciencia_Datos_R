# Calculadora sofisticada

Fecha de la última revisión
```{r c04-1, echo=FALSE}
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)

Sys.Date()
```

***

Activar los siguientes paquetes

```{r c04-2}
library(tidyverse)
library(datos)
```



El tema proviene de los siguientes sitios.

English:
 <https://r4ds.had.co.nz/workflow-basics.html>
 
Español:
 <https://es.r4ds.hadley.nz/04-workflow-basics.html>
 
 
***



## Temas:

  - Conocimientos básicos de programación
  - La importancia de los nombres
  - Usando funciones
  
### Sumar  
  
```{r c04-3}
2+2
```

### Exactamente como una calculadora

```{r c04-4}
(23+3+3)^2+2
```


### Trigonometría

```{r c04-5}
sin(pi/3)
```


### Log natural y a base 10


```{r c04-6}
log(100)
log10(100)
```

### Asignaciones de variables

::: {.peligro}
Para asignar un valor puedes usar `<-` o `=`, pero para **comparar** dos cosas se usa `==` (doble igual). Confundir `=` con `==` es uno de los errores más comunes al empezar. Encontrarás más errores frecuentes y cómo resolverlos en el capítulo *Errores comunes* (\@ref(errores)).
:::

```{r c04-7}
x<- 4
x
y=3
y

x/y
```


### Variables 


- la función "seq" para secuencia

::: {.funcion}
**`seq()`, `c()`, `length()`** — crear y medir vectores.

- **`c(1, 2, 3)`:** combina valores en un vector; **`11:20`** genera una secuencia consecutiva.
- **`seq(101, 110)`:** secuencia con más control (inicio, fin, paso).
- **`length(x)`:** cuántos elementos tiene un vector.
:::

```{r c04-8}
edad_nd=seq(101, 110)
edad_nd
```

- variables asignada

```{r c04-9}
edad_uprh=c(11:20)
edad_uprh
```

### Unir variables en un data frame

```{r c04-10}
edad=data.frame(edad_nd, edad_uprh)
edad
```


```{r c04-11}
edad$diff=edad$edad_nd-edad$edad_uprh
edad


edad$tres=edad$edad_uprh^3
edad
```


### Errores cuando a variables no tienen la misma cantidad de entradas


```{r c04-12}
edad_nd_1=base::seq(101, 111)
edad_nd_1
```

- variables asignada

```{r c04-13}
edad_uprh_1=c(11:20, NA)
edad_uprh_1
```

### Unir variables en un data frame

```{r c04-14}
edad_1=data.frame(edad_nd_1, edad_uprh_1)
edad_1
```


### Cuantos valores hay en una variable

```{r c04-15}
length(edad_uprh)

length(edad$tres)
length(edad)
```


### Seleccionar solamente parte de los datos de un archivo

- Primero visualizar los datos

```{r c04-16}
library(datos)
millas
```

#### Seleccionar los carros que tienen cilindros igual a 8 (solamente)

::: {.historia}
El pipe `%>%` lo creó Stefan Milton Bache en el paquete `magrittr` (2014); su nombre juega con el cuadro de Magritte "Ceci n'est pas une pipe". Desde 2021, R incluye su propio pipe nativo `|>`.
:::

La función de Pipe.    %>% o |>

::: {.funcion}
**`select()`** y **`filter()`** (paquete **dplyr**) — elegir columnas y filas.

- **`select(columna)`:** se queda con las **columnas** indicadas.
- **`filter(condición)`:** se queda con las **filas** que cumplen una condición (p. ej. `cilindros == 8`).
- **El pipe `|>` / `%>%`:** encadena pasos, pasando el resultado de uno como entrada del siguiente.
:::



```{r c04-17}
library(tidyverse)
millas
names(millas)

millas |> dplyr::select(cilindros) |> 
  dplyr::filter(cilindros == 8)

```





#### Seleccionar los diamantes mayor de 3 quilate

```{r c04-18}
diamantes |> dplyr::select(quilate) |> 
  filter(quilate > 3)
```


#### Seleccionar los diamantes con igual o mayor de 4000 precio

```{r c04-19, echo=FALSE}
#unique(diamantes$precio)

dia_4000 = diamantes |> dplyr::select(precio) |> 
  dplyr::filter(precio >= 4000)

head(dia_4000)
```


***
#### Contabiliza cuantos diamantes tienen un valor de $4000 o más


```{r c04-20, contar las cantidad lineas}

nrow(dia_4000) 

```




