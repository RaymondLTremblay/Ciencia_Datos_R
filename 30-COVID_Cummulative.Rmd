# (APPENDIX) Apéndice {.unnumbered}

# Apéndice A: COVID-19 acumulativo {.unnumbered}

```{r c30-1, echo=FALSE}
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)

Sys.Date()
```
------------------------------------------------------------------------

*** 

## COVID 19 Cumulativo {-}


Usa los datos de COVID-19 en Puerto Rico (PR) del Departamento de Salud de PR.




```{r c30-2}
url_COVID_PR <- read_csv("Datos/url_COVID_PR.csv")

head(url_COVID_PR)


```

***

1. Calcula los promedios en periodos de 7 (días; promedio corriendo) y añádele a tu dataframe

::: {.nota}
Un promedio *cumulativo* (`cummean`) usa todos los datos desde el inicio hasta cada fecha, mientras que un promedio *corriendo* o móvil (`rollmean` con ventana de 7 días) solo promedia los días más cercanos.
:::

::: {.funcion}
**`cummean()`** (paquete **dplyr**) y **`rollmean()`** (paquete **zoo**) — resumen una serie de valores a lo largo del tiempo.

- **`cummean(x)`:** devuelve el promedio *acumulado* — cada valor es el promedio de todos los datos hasta esa fila.
- **`rollmean(x, k = 7, fill = NA, align = "left")`:** el promedio *móvil* de una ventana de `k` días; `fill = NA` rellena los extremos donde no hay ventana completa y `align` fija dónde se ancla la ventana.
- **`rollapply(x, width, FUN)`:** versión general que aplica *cualquier* función (aquí `var`) sobre una ventana móvil.
:::

_ Usa la columna de IncCasosSaludNuevo para todos los cálculos

- Enseñe las primeras 20 filas de tu dataframe con las nuevas columnas añadidas




```{r c30-3, warning=FALSE, message=FALSE, echo=FALSE}
Cummulativo=url_COVID_PR %>% 
  dplyr::select(Fecha, IncCasosSaludNuevo) |> 
  dplyr::select(IncCasosSaludNuevo) %>% 
  mutate(Prom_cum=cummean(IncCasosSaludNuevo)) %>% # dplyr:: es para especificar que la función cummean es del paquete dplyr
  mutate(Var_cum=TidyDensity::cvar(IncCasosSaludNuevo)) |> 
  mutate(promedio_coriendo= zoo::rollmean(IncCasosSaludNuevo, k=7, fill=NA, align="left")) |> 
  mutate(varianza_coriendo= zoo::rollapply(IncCasosSaludNuevo, width=7, FUN=var, fill=NA, align="left"))



```

***


2. Haz una figura que muestra el cambio en el promedio cumulativo corriendo de casos nuevos.
```{r c30-4, warning=FALSE, message=FALSE, echo=FALSE}

 
  ggplot(Cummulativo, aes(x=1:nrow(url_COVID_PR))) +
  geom_line(aes(y=promedio_coriendo), color="blue") +
  labs(title="Promedio Cumulativo Corriendo de Casos Nuevos de COVID-19 en PR",
       x="Días",
       y="Promedio Cumulativo Corriendo de Casos Nuevos") +
  theme_minimal()


```

